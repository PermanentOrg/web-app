import { PrConstantsPipe } from './pr-constants.pipe';

describe('PrConstantsPipe', () => {
  it('create an instance', () => {
    const pipe = new PrConstantsPipe();
    expect(pipe).toBeTruthy();
  });
});
